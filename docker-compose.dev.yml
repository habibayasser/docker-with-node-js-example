version: "3.8"
# services:
#   node-app:
#     container_name: express-node-app-container
#     build: .
#     ports:
#       - "4000:4000"
#     volumes:
#       - ./src:/app/src/:ro
#     environment:
#       - NODE_ENV=development
#     env_file:
#       - ./.env

services:
  node-app:
    build:
      target: development
    volumes:
    - ./src:/app/src/:ro
    environment:
      - NODE_ENV=dvelopment
    command: npm run start-dev
